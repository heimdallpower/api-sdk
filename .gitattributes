# Shell scripts must keep LF endings in the object store and on checkout —
# they are executed by bash on ubuntu-latest runners, where a CR would abort
# the interpreter. This does not depend on any contributor's core.autocrlf.
*.sh text eol=lf
